# ==========================================================
# NeerVibe — Backend & ML Dependencies
# Python 3.11+
# ==========================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# --- Data validation / settings ---
pydantic==2.10.3
pydantic-settings==2.7.0

# --- Database ---
sqlalchemy==2.0.36
greenlet==3.1.1
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
geoalchemy2==0.16.0

# --- Cache / real-time state ---
redis==5.2.1

# --- WebSockets ---
websockets==14.1

# --- Geospatial ---
geopandas==1.0.1
shapely==2.0.6

# --- Data science / numeric ---
# Python 3.13 requires numpy 2.x / pandas 2.3+ (no cp313 wheels on the 1.26/2.2 lines).
numpy==2.2.6
pandas==2.3.3

# --- Machine learning ---
# Python 3.13 requires TensorFlow 2.20+ (no 2.18 wheel exists for cp313).
tensorflow==2.21.0
keras>=3.12.0
scikit-learn==1.7.2

# --- Auth-ready (JWT, hashing) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Utilities ---
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0
faker==33.1.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
