__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
*.egg-info/

# Node / frontend
frontend/node_modules/
frontend/.next/
frontend/out/

# ML artifacts (large binaries — regenerate via training scripts).
# eta_predictor_v1 is committed on purpose: it's ~86KB and the deployed
# backend needs it available at boot without a training step.
ml/models/artifacts/*
!ml/models/artifacts/eta_predictor_v1/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Railway IaC config — contains provisioning secrets for this deployment
# session (DB password, SECRET_KEY), not meant as team-shared infra code.
.railway/
# npm scaffolding for the `railway` TS SDK the above depends on
node_modules/
/package.json
/package-lock.json
render/
.render-cli/
